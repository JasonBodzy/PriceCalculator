public class Parser {
    String closePrices;
    public Parser(String closePrices) {
        this.closePrices = closePrices; //txt file with close prices
    }

    double[] getClosePrices() { // parses and makes an array of close prices
        double[] closePricesArray = {};
        return closePricesArray;
    }
}
